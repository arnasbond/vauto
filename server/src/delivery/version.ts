/**
 * Stage 11G — Delivery Integration version pin (11G.3 → 1.2).
 */

export const DELIVERY_INTEGRATION_VERSION = "1.2" as const;
export type DeliveryIntegrationVersion = typeof DELIVERY_INTEGRATION_VERSION;

/** Transaction State Machine version — Stage 11A. */
export const TRANSACTION_STATE_MACHINE_VERSION = "1.0" as const;

export type TransactionStateMachineVersion =
  typeof TRANSACTION_STATE_MACHINE_VERSION;

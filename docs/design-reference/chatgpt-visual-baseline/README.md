# VAUTO MASTER Visual Reference — Authoritative Source of Truth

## Files

### `VAUTO-MASTER-LIGHT.jpg`
**OWNER-APPROVED MASTER LIGHT / AUTHORITATIVE VISUAL SOURCE OF TRUTH.**

- SHA-256: `a9ce7a3ced140e710e1b16e2c7db031649ddc21cc2a32697a62a2b8f17461f7d`
- Size: 139,340 bytes
- Dimensions: 1024 × 682
- Ingested (owner-supplied original, byte-for-byte copy verified): 2026-08-26

### `VAUTO-MASTER-DARK.jpg`
**OWNER-APPROVED MASTER DARK / AUTHORITATIVE VISUAL SOURCE OF TRUTH.**

- SHA-256: `8e8347f1d661327adabf7e4ca54df034209336b7a0bb021a69d5182ad4b368ff`
- Size: 150,000 bytes
- Dimensions: 1024 × 682
- Ingested (owner-supplied original, byte-for-byte copy verified): 2026-08-26

## Status

Both images are copied **byte-for-byte, unmodified** from the owner-supplied originals. They have
**not** been redesigned, reinterpreted, regenerated, enhanced, compressed, resized, re-exported,
or recolored in any way. Do not replace these files with a re-export, a resave through an image
editor, or a "cleaned up" version — any future update must go through the same explicit
owner-ingestion process with a fresh SHA-256 record in this file.

## What these images govern

`VAUTO-MASTER-LIGHT.jpg` and `VAUTO-MASTER-DARK.jpg` together define **one unified VAUTO Design
System 2.0**. DARK is not an independent redesign — it is the same system's dark counterpart.

They are the authoritative reference for:

- composition and information hierarchy
- spacing and layout rhythm
- typography (family, scale, weight)
- component depth (cards, shadows, borders, radii)
- imagery treatment
- responsive visual language (as shown across the device frames depicted)
- restrained emerald accent usage
- LIGHT/DARK structural and visual parity

## What these images do NOT override

- They are **references, not pixel-literal requirements** where responsive behavior,
  accessibility (contrast, focus, touch targets), real production data, or functional
  constraints require adaptation.
- **Functional architecture and frozen authority boundaries always remain authoritative** —
  this includes AI authority, **Human-in-the-Loop (HITL) authority** (`HUMAN_CONFIRMED` /
  `HUMAN_OVERRIDDEN` always outrank AI suggestions), Listing Intelligence contracts, Phase A/B/C
  AI-maturity behavior, transaction authority, payments/ledger, reputation authority, Deal Room
  backend contracts, category/domain source-of-truth, and the **Stage 11 / Stage 11J frozen
  invariants** (the certified transaction/payment file-hash fingerprint — see
  `docs/checkpoints/stage11-frozen-baseline.txt`). Wherever a purely illustrative detail in either
  JPG would conflict with any of the above, the functional/frozen contract wins and the visual
  reference must be adapted, never the other way around. Visual convergence work wraps this
  existing certified behavior; it never redesigns it.

## What is explicitly NOT a competing reference

- Historical VAUTO screenshots (`docs/ui-home-3.0/`, `docs/ui-final-9.0/`, `docs/ui-nav-2.0/`,
  `docs/ui-market-4.0/`, `docs/ui-detail-5.0/`, `docs/ui-profile-6.0/`, `docs/ui-business-7.0/`,
  `docs/ui-control-center-8.0/`, etc.) may be used **only as implementation-history evidence**
  (what the product looked like at a given stage) — **never** as an alternative or competing
  MASTER visual source of truth.
- Legacy external-portal simulations (Autoplius, Aruodas, Skelbiu, Vinted, and any
  "Chameleon"-branded portal-simulator UI in `src/lib/chameleon-themes.ts` /
  `src/lib/chameleon-portal-ui.ts`) are **explicitly non-authoritative** and must never be used
  as a visual reference for MASTER convergence work.

## Provenance

Supplied directly by the project owner (Arnas) as `29153.jpg` (MASTER LIGHT) and `29152.jpg`
(MASTER DARK) via chat attachment during the MASTER REFERENCE INGESTION GATE. The attached files
were treated as the authoritative originals per explicit instruction; no on-disk or repo-history
file of a similar name was substituted.

/** Existing VAUTO-owned photography. Decorative; theme selected by the DS token. */
export function HomeHeroAtmosphere() {
  return <div aria-hidden data-home-hero-atmosphere className="pointer-events-none absolute inset-0 select-none bg-cover bg-bottom bg-no-repeat" style={{ backgroundImage: "var(--hero-atmosphere-bg)" }} />;
}

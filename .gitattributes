# Stage 11 frozen fingerprint baseline must remain byte-for-byte identical to the
# originally certified manifest forever — never let autocrlf rewrite its line endings.
docs/checkpoints/stage11-frozen-baseline.txt -text eol=lf

# MASTER visual references are the owner-approved authoritative source of truth —
# treat as pure binary, never subject to any text/EOL conversion.
docs/design-reference/chatgpt-visual-baseline/*.jpg -text binary
